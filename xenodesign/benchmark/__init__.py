"""Benchmark harness scaffolding (spec §2): the 3-case registry, per-case config,
Chai .restraints builders, and the case_metrics glue. CPU-only; no chai/torch at import.
GPU case-runs and the 9DXX target gate (#29) are DEFERRED — see cases.py / case_metrics.py.
"""
